package com.tony.kingdetective.config;

import org.springframework.context.annotation.Configuration;

/**
 * Web UI configuration placeholder.
 * SPA resource handling is configured in CrossDomainFilter.
 */
@Configuration
public class WebUiConfig {
    // Resource handling and SPA routing is managed in CrossDomainFilter.addResourceHandlers()
}
